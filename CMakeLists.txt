cmake_minimum_required(VERSION 3.0)
project(ktm VERSION 0.0.1 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
aux_source_directory(src SRC_DIR)
include_directories(include)
add_executable(${PROJECT_NAME} ${SRC_DIR})
